# 更新记录

## v0.0.46

- EVM 区块扫描失败后恢复为将原区块范围重新入队，不再因达到 5 次上限而丢弃。
- TRON 区块重试恢复为持续延迟重试，不再因达到 5 次上限而停止。
- Aptos 恢复作者源码的错误分支处理：响应读取或 JSON 异常时重新入队，请求失败或 HTTP 状态异常时直接返回。
- TON 回溯恢复为全部历史区块扫描成功后再标记订单完成回溯，中途失败或取消时保留后续重试机会。
- 更新对应的 EVM、TRON、Aptos 和回溯测试；Docker 发布继续仅构建 `linux/amd64`。

## v0.0.12

- Telegram 启动通知改用 `smpay` 名称和 `v` 版本前缀，移除频道与社区按钮，并保留二开仓库预览链接。
- 将登录页、后台项目链接、RPC、金额匹配、MQTT 和 USDC 支付页面的项目地址统一为二开仓库。
- 删除官方和 LangGe 收银台，仅保留 SM 收银台；旧配置会自动回退到 SM 模板。
- 移除后台 MQTT 设置标签和 RPC 说明按钮，保留后端 MQTT 功能。
- 基本设置头像改为 sm23444 的 GitHub 头像。

## v0.0.11

- 将 `Payment-Gateway` 和 `Powered-By` 请求头中的项目地址改为二开仓库：`https://github.com/sm23444/newbepu`。

## v0.0.10

- 后台页脚改为显示 `年份 © smpay · v版本号`。
- Docker 构建时自动将 `VERSION` 注入前端，后续发布无需手动修改页脚版本。
- 移除页脚中的 `All rights reserved.`，并将点击链接改为二开仓库。

## v0.0.9

- 将 Telegram 启动欢迎消息中的源码地址改为二开仓库：`https://github.com/sm23444/newbepu`。

## v0.0.8

- 为“同步记录”“汇率配置”“基本设置”“区块节点”更换为明显的彩色 SVG 菜单图标。
- 将汇率监控和系统管理的原折叠菜单保持为一级菜单。
- 删除后台“说明文档”菜单及对应页面。
